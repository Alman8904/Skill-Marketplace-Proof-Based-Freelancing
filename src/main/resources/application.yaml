spring:
  application:
    name: SM

  datasource:
    url: jdbc:postgresql://localhost:8000/SM
    username: postgres
    password: 890400

  jpa:
    hibernate:
      ddl-auto: create

  flyway:
    enabled: false
    baseline-on-migrate: false

jwt:
  secret: manidontknowmyownsecretkeyanymore12345
  expiration: 3600000

app:
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin123}
    email: ${ADMIN_EMAIL:admin@mail.com}
